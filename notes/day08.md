# DAY8：接口自动化框架架构设计

## 今日完成
- 建立三层架构：`api/`（接口层）、`config/`（配置层）、`testcases/`（用例层）
- 封装 `BaseAPI` 公共请求逻辑
- 封装 `PostsAPI` 业务接口
- 测试用例干净到只包含业务语言
- 6 个用例全部通过（6 passed）

## 学到的重要概念
- 分层架构：按职责拆分代码，避免混乱
- 封装：把底层请求细节隐藏，测试用例只关心业务
- 配置外置：环境地址统一管理，换环境只改一个文件

## 运行状态
- `pytest testcases/test_posts.py -v` → 6 passed ✅

## 明天计划
- 继续完善框架：增加更丰富的断言方法
- 学习接口鉴权处理（Token）